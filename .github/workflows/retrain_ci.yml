name: retrain_ci

on:
  workflow_dispatch:
  push:
    paths:
      - 'MLProject/**'

jobs:
  retrain:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder retrain workflow
        run: echo "Add MLflow, Docker build, and Docker Hub push steps here"
